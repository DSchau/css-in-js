language: node_js
node_js:
  - stable
cache:
  yarn: true

script: yarn test

before_deploy:
  - yarn build
  - ls src/slides/intro/assets/images
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: dist
  on:
    branch: master
